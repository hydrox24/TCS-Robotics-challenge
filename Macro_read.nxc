#define RECORDS 2000
#define RECORD_SIZE 6
#define FILE_SIZE (RECORD_SIZE * RECORDS)
#define FILE_NAME "movement.dat"

#include "file_system.nxc"

task main()
{
	open_for_read();
	movement m;
	movement current;
	read_all(m,current);
	RotateMotor(OUT_A,75,m.stepA);
	RotateMotor(OUT_C,75,m.stepC);
}
